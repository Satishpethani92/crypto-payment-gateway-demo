{
  "name": "crypto-payment-gateway-demo",
  "version": "1.0.0",
  "description": "Demo merchant store — real integration prototype for Crypto Payment Gateway APIs",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
